{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "export LD_LIBRARY_PATH=/nix/var/nix/profiles/default/lib:$LD_LIBRARY_PATH && export GI_TYPELIB_PATH=/nix/var/nix/profiles/default/lib/girepository-1.0 && . /opt/venv/bin/activate && python manage.py migrate && python manage.py create_superuser_from_env && gunicorn config.wsgi:application --bind [::]:$PORT --workers 2 --timeout 120",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
